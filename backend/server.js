require("dotenv").config();const express=require("express");const mongoose=require("mongoose");const cors=require("cors");const path=require("path");const app=express();app.use(cors());app.use(express.json());mongoose.connect(process.env.MONGO_URI).then(()=>console.log("DB")).catch(e=>console.log(e));const User=require("./models/User");app.post("/register",async(req,res)=>{const{phone,password}=req.body;const u=await User.create({phone,password,role:"student"});res.json(u)});app.post("/login",async(req,res)=>{const{phone,password}=req.body;const u=await User.findOne({phone,password});if(!u||u.banned)return res.status(403).json({error:"Denied"});res.json(u)});app.get("/admin/stats",async(req,res)=>{const users=await User.countDocuments();const banned=await User.countDocuments({banned:true});res.json({users,banned})});app.post("/admin/ban/:id",async(req,res)=>{const u=await User.findById(req.params.id);u.banned=true;await u.save();res.json(u)});app.use(express.static(path.join(__dirname,"../frontend")));app.get("/",(req,res)=>res.sendFile(path.join(__dirname,"../frontend/index.html")));const PORT=process.env.PORT||3000;app.listen(PORT,()=>console.log(PORT));